⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## What's Changed

### ✨ New Features
- Auto-detect now computes the Z height directly from the IR probe, removing the need for manual Z measurement

### 🔧 Improvements
- Simplified tool positioning steps for a clearer, easier setup process
- Editor now uses the shared ncSender dark and light themes for a consistent look
